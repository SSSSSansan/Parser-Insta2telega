name: ESG Parser

on:
  schedule:
    - cron: '0 */2 * * *'   # каждые 2 часа
  workflow_dispatch:         # чтобы можно было запускать вручную кнопкой

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install --with-deps chromium

      - name: Restore Instagram cookies
        run: echo "${{ secrets.IG_COOKIES_B64 }}" | base64 -d > instagram_cookies.json

      - name: Run parser
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHANNEL_ID: ${{ secrets.TG_CHANNEL_ID }}
          IG_USERNAME: ${{ secrets.IG_USERNAME }}
        run: python main.py --once

      - name: Save updated database back to repo
        run: |
          git config user.name "esg-bot"
          git config user.email "bot@users.noreply.github.com"
          git add posts.db
          git diff --staged --quiet || git commit -m "chore: update posts.db"
          git push